import history from './cards/history';
import culture from './cards/culture';
import territory from './cards/territory';
import feasts from './cards/feasts';

const cards = {
	history: history,
	culture: culture,
	territory: territory,
	feasts: feasts
};

export default cards;
